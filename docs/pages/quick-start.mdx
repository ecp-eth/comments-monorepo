# Quick Start

Get started with the Ethereum Comments Protocol in minutes.

## Installation

Run one of the commands below based on your preferred package manager and then follow the steps in the terminal.

:::code-group

```bash [npm]
npm install @ecp.eth/sdk
# install peer dependencies
npm install viem wagmi @tanstack/react-query @tanstack/query-core
```

```bash [yarn]
yarn add @ecp.eth/sdk
# install peer dependencies
yarn add viem wagmi @tanstack/react-query @tanstack/query-core
```

```bash [pnpm]
pnpm add @ecp.eth/sdk
# install peer dependencies
pnpm add viem wagmi @tanstack/react-query @tanstack/query-core
```

:::

## Basic Usage

::::steps

### Install dependencies

```bash
npm install @ecp.eth/sdk wagmi
```

### Render the comment box

Render `<CommentsEmbed />` component with a unique url for current page.

```tsx
import { CommentsEmbed } from "@ecp.eth/sdk/react";

const baseOrigin = "https://blog.example.com";

export function Comments() {
  return (
    <div>
      <h3>Comments</h3>
      <CommentsEmbed
        // the uri for commenting
        uri={new URL(window.location.pathname, baseOrigin)}
      />
    </div>
  );
}
```

For more details, see [integration options: react component library](/integration-options/react-component-library) and [`<CommentsEmbed />`](/sdk-reference/react/functions/CommentsEmbed).

### Deploy and start interacting with your community!

::::

## Next Steps

- Learn more about [architecture overview](/architecture-overview) and [post comment flows](/post-comment-flows)
- Explore [integration options](/integration-options/)
- Check out our [demo repository](https://github.com/ecp-eth/comments-monorepo/tree/main/apps/demo)
