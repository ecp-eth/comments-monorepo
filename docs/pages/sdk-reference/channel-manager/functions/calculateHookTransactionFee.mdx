[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [channel-manager](/sdk-reference/channel-manager/index.mdx) / calculateHookTransactionFee

# Function: calculateHookTransactionFee()

```ts
function calculateHookTransactionFee(params): Promise<CalculateHookTransactionFeeResult>;
```

Defined in: [packages/sdk/src/channel-manager/hook.ts:263](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/channel-manager/hook.ts#L263)

Calculates the hook transaction fee and returns the hook value after deducting the protocol fee.
If the value is 0 or if the hook transaction fee percentage is 0, returns the original value.

## Parameters

### params

[`CalculateHookTransactionFeeParams`](/sdk-reference/channel-manager/type-aliases/CalculateHookTransactionFeeParams.mdx)

The parameters for calculating the hook transaction fee

## Returns

`Promise`\<[`CalculateHookTransactionFeeResult`](/sdk-reference/channel-manager/type-aliases/CalculateHookTransactionFeeResult.mdx)\>

The transaction hash and the hook value after fee deduction
