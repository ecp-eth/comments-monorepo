[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / FetchCommentRepliesOptions

# Type Alias: FetchCommentRepliesOptions

> **FetchCommentRepliesOptions**: `object`

Defined in: [packages/sdk/src/api.ts:154](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/api.ts#L154)

The options for `fetchCommentReplies()`

## Type declaration

### apiUrl?

> `optional` **apiUrl**: `string`

URL on which /api/comments/$commentId/replies endpoint will be called

#### Default

```ts
"https://api.ethcomments.xyz"
```

### appSigner?

> `optional` **appSigner**: [`Hex`](/sdk-reference/schemas/type-aliases/Hex.mdx)

Filters only to comments sent using this app signer key.

### commentId

> **commentId**: [`Hex`](/sdk-reference/schemas/type-aliases/Hex.mdx)

The ID of the comment to fetch replies for

### cursor?

> `optional` **cursor**: [`Hex`](/sdk-reference/schemas/type-aliases/Hex.mdx)

The cursor to fetch comments from

### limit?

> `optional` **limit**: `number`

#### Default

```ts
50
```

### retries?

> `optional` **retries**: `number`

Number of times to retry the signing operation in case of failure.

#### Default

```ts
3
```

### signal?

> `optional` **signal**: `AbortSignal`

### sort?

> `optional` **sort**: `"asc"` \| `"desc"`

#### Default

```ts
"desc"
```

### viewer?

> `optional` **viewer**: [`Hex`](/sdk-reference/schemas/type-aliases/Hex.mdx)

The viewer's address. This is useful when the content moderation is enabled on the indexer.
