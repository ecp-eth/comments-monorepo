[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentTypedData

# Function: createCommentTypedData()

```ts
function createCommentTypedData(__namedParameters): object;
```

Defined in: [packages/sdk/src/utils.ts:81](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L81)

Create the EIP-712 typed data structure for adding comment

## Parameters

### \_\_namedParameters

#### chainId

`number`

#### commentData

\{
  `appSigner`: `` `0x${string}` ``;
  `author`: `` `0x${string}` ``;
  `content`: `string`;
  `deadline`: `bigint`;
  `metadata`: `string`;
  `nonce`: `bigint`;
  `parentId`: `` `0x${string}` ``;
  `targetUri`: `string`;
\}

#### commentData.appSigner

`` `0x${string}` `` = `HexSchema`

#### commentData.author

`` `0x${string}` `` = `HexSchema`

#### commentData.content

`string` = `...`

#### commentData.deadline

`bigint` = `...`

#### commentData.metadata

`string` = `...`

#### commentData.nonce

`bigint` = `...`

#### commentData.parentId

`` `0x${string}` `` = `HexSchema`

#### commentData.targetUri

`string` = `...`

## Returns

`object`

The typed data

### domain

```ts
domain: object;
```

#### domain.chainId

```ts
chainId: number;
```

#### domain.name

```ts
name: "Comments";
```

#### domain.verifyingContract

```ts
verifyingContract: "0xabD9cE1952992211dEe051Df6ed337fa6efC995d";
```

#### domain.version

```ts
version: "1";
```

### message

```ts
message: object = CommentDataSchema;
```

#### message.appSigner

```ts
appSigner: `0x${string}` = HexSchema;
```

#### message.author

```ts
author: `0x${string}` = HexSchema;
```

#### message.content

```ts
content: string;
```

#### message.deadline

```ts
deadline: bigint;
```

#### message.metadata

```ts
metadata: string;
```

#### message.nonce

```ts
nonce: bigint;
```

#### message.parentId

```ts
parentId: `0x${string}` = HexSchema;
```

#### message.targetUri

```ts
targetUri: string;
```

### primaryType

```ts
primaryType: "AddComment";
```

### types

```ts
types: object;
```

#### types.AddComment

```ts
AddComment: (
  | {
  name: "content";
  type: "string";
}
  | {
  name: "metadata";
  type: "string";
}
  | {
  name: "targetUri";
  type: "string";
}
  | {
  name: "parentId";
  type: "bytes32";
}
  | {
  name: "author";
  type: "address";
}
  | {
  name: "appSigner";
  type: "address";
}
  | {
  name: "nonce";
  type: "uint256";
}
  | {
  name: "deadline";
  type: "uint256";
})[];
```
