[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentTypedData

# Function: createCommentTypedData()

> **createCommentTypedData**(`__namedParameters`): `object`

Defined in: [packages/sdk/src/utils.ts:143](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L143)

Create the EIP-712 typed data structure for adding comment

## Parameters

### \_\_namedParameters

#### chainId

`number`

#### commentData

\{ `appSigner`: `` `0x${string}` ``; `author`: `` `0x${string}` ``; `content`: `string`; `deadline`: `bigint`; `metadata`: `string`; `parentId`: `` `0x${string}` ``; `salt`: `` `0x${string}` ``; `targetUri`: `string`; \}

#### commentData.appSigner

`` `0x${string}` `` = `HexSchema`

#### commentData.author

`` `0x${string}` `` = `HexSchema`

#### commentData.content

`string` = `...`

#### commentData.deadline

`bigint` = `...`

#### commentData.metadata

`string` = `...`

#### commentData.parentId

`` `0x${string}` `` = `HexSchema`

#### commentData.salt

`` `0x${string}` `` = `HexSchema`

#### commentData.targetUri

`string` = `...`

## Returns

`object`

The typed data

### domain

> **domain**: `object`

#### domain.chainId

> **chainId**: `number`

#### domain.name

> **name**: `"Comments"`

#### domain.verifyingContract

> **verifyingContract**: `"0x4b2fdb900fd003e30919e612d75046823b879554"`

#### domain.version

> **version**: `"1"`

### message

> **message**: `object` = `CommentDataSchema`

#### message.appSigner

> **appSigner**: `` `0x${string}` `` = `HexSchema`

#### message.author

> **author**: `` `0x${string}` `` = `HexSchema`

#### message.content

> **content**: `string`

#### message.deadline

> **deadline**: `bigint`

#### message.metadata

> **metadata**: `string`

#### message.parentId

> **parentId**: `` `0x${string}` `` = `HexSchema`

#### message.salt

> **salt**: `` `0x${string}` `` = `HexSchema`

#### message.targetUri

> **targetUri**: `string`

### primaryType

> **primaryType**: `"AddComment"`

### types

> **types**: `object`

#### types.AddComment

> **AddComment**: (\{ `name`: `"content"`; `type`: `"string"`; \} \| \{ `name`: `"metadata"`; `type`: `"string"`; \} \| \{ `name`: `"targetUri"`; `type`: `"string"`; \} \| \{ `name`: `"parentId"`; `type`: `"bytes32"`; \} \| \{ `name`: `"author"`; `type`: `"address"`; \} \| \{ `name`: `"appSigner"`; `type`: `"address"`; \} \| \{ `name`: `"salt"`; `type`: `"bytes32"`; \} \| \{ `name`: `"deadline"`; `type`: `"uint256"`; \})[]
