[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentData

# Function: createCommentData()

> **createCommentData**(`__namedParameters`): [`CommentData`](/sdk-reference/types/type-aliases/CommentData.mdx)

Defined in: [utils.ts:18](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L18)

Create the data structure of a comment

## Parameters

### \_\_namedParameters

#### appSigner

`` `0x${string}` ``

The address of the app signer

#### author

`` `0x${string}` ``

The address of the author of the comment

#### content

`string`

The content of the comment

#### deadline?

`bigint`

The deadline of the comment submission in seconds since epoch

#### metadata?

`object`

Metadata about the comment

#### nonce

`bigint`

The current nonce of the user on the chain

#### parentId?

`` `0x${string}` ``

The ID of the parent comment

#### targetUri?

`string`

The URI of the page the comment is about

## Returns

[`CommentData`](/sdk-reference/types/type-aliases/CommentData.mdx)

The data structure of a comment
