[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentData

# Function: createCommentData()

> **createCommentData**(`__namedParameters`): `object`

Defined in: [packages/sdk/src/utils.ts:97](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L97)

Create the data structure of a comment

## Parameters

### \_\_namedParameters

#### appSigner

`` `0x${string}` ``

The address of the app signer

#### author

`` `0x${string}` ``

The address of the author of the comment

#### content

`string`

The content of the comment

#### deadline?

`bigint`

The deadline of the comment submission in seconds since epoch

#### metadata?

`object`

Metadata about the comment

#### parentId?

`` `0x${string}` ``

The ID of the parent comment

#### salt?

`` `0x${string}` ``

The current nonce of the user on the chain

#### targetUri?

`string`

The URI of the page the comment is about

## Returns

`object`

The data structure of a comment

### appSigner

> **appSigner**: `` `0x${string}` `` = `HexSchema`

### author

> **author**: `` `0x${string}` `` = `HexSchema`

### content

> **content**: `string`

### deadline

> **deadline**: `bigint`

### metadata

> **metadata**: `string`

### parentId

> **parentId**: `` `0x${string}` `` = `HexSchema`

### salt

> **salt**: `` `0x${string}` `` = `HexSchema`

### targetUri

> **targetUri**: `string`
