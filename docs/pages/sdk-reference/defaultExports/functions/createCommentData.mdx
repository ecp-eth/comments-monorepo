[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentData

# Function: createCommentData()

```ts
function createCommentData(__namedParameters): {
  appSigner: `0x${string}`;
  author: `0x${string}`;
  content: string;
  deadline: bigint;
  metadata: string;
  nonce: bigint;
  parentId: `0x${string}`;
  targetUri: string;
};
```

Defined in: [packages/sdk/src/utils.ts:36](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L36)

Create the data structure of a comment

## Parameters

### \_\_namedParameters

#### appSigner

`` `0x${string}` ``

The address of the app signer

#### author

`` `0x${string}` ``

The address of the author of the comment

#### content

`string`

The content of the comment

#### deadline?

`bigint`

The deadline of the comment submission in seconds since epoch

#### metadata?

`object`

Metadata about the comment

#### nonce

`bigint`

The current nonce for the user per app on the chain

#### parentId?

`` `0x${string}` ``

The ID of the parent comment

#### targetUri?

`string`

The URI of the page the comment is about

## Returns

```ts
{
  appSigner: `0x${string}`;
  author: `0x${string}`;
  content: string;
  deadline: bigint;
  metadata: string;
  nonce: bigint;
  parentId: `0x${string}`;
  targetUri: string;
}
```

[CommentData](/sdk-reference/schemas/type-aliases/CommentData.mdx) The data structure of a comment

### appSigner

```ts
appSigner: `0x${string}` = HexSchema;
```

### author

```ts
author: `0x${string}` = HexSchema;
```

### content

```ts
content: string;
```

### deadline

```ts
deadline: bigint;
```

### metadata

```ts
metadata: string;
```

### nonce

```ts
nonce: bigint;
```

### parentId

```ts
parentId: `0x${string}` = HexSchema;
```

### targetUri

```ts
targetUri: string;
```
