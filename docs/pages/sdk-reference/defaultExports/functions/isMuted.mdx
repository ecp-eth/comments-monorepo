[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

---

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / isMuted

# Function: isMuted()

> **isMuted**(`options`): `Promise`\<`boolean`\>

Defined in: [packages/sdk/src/api.ts:341](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/api.ts#L341)

Checks if an address is marked as a muted on the indexer of your choice.

## Parameters

### options

[`IsMutedOptions`](/sdk-reference/defaultExports/type-aliases/IsMutedOptions.mdx)

The options for checking if an address is muted

## Returns

`Promise`\<`boolean`\>

A promise that resolves to `true` if the address is marked as muted, `false` otherwise
