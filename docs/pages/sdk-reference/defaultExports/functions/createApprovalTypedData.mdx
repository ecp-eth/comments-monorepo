[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createApprovalTypedData

# Function: createApprovalTypedData()

```ts
function createApprovalTypedData(__namedParameters): object;
```

Defined in: [packages/sdk/src/utils.ts:107](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L107)

Create the EIP-712 typed data structure for approving comment

## Parameters

### \_\_namedParameters

#### appSigner

`` `0x${string}` ``

#### author

`` `0x${string}` ``

#### chainId

`number`

#### deadline?

`bigint`

#### nonce

`bigint`

## Returns

`object`

The typed data

### domain

```ts
domain: object;
```

#### domain.chainId

```ts
chainId: number;
```

#### domain.name

```ts
name: "Comments";
```

#### domain.verifyingContract

```ts
verifyingContract: "0xabD9cE1952992211dEe051Df6ed337fa6efC995d";
```

#### domain.version

```ts
version: "1";
```

### message

```ts
message: object;
```

#### message.appSigner

```ts
appSigner: `0x${string}` = HexSchema;
```

#### message.author

```ts
author: `0x${string}` = HexSchema;
```

#### message.deadline

```ts
deadline: bigint;
```

#### message.nonce

```ts
nonce: bigint;
```

### primaryType

```ts
primaryType: "AddApproval";
```

### types

```ts
types: object;
```

#### types.AddApproval

```ts
AddApproval: (
  | {
  name: "author";
  type: "address";
}
  | {
  name: "appSigner";
  type: "address";
}
  | {
  name: "nonce";
  type: "uint256";
}
  | {
  name: "deadline";
  type: "uint256";
})[];
```
