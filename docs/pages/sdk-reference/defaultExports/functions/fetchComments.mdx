[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / fetchComments

# Function: fetchComments()

```ts
function fetchComments(options): Promise<{
  extra: {
     moderationEnabled: boolean;
  };
  pagination: {
     endCursor: `0x${string}`;
     hasNext: boolean;
     hasPrevious: boolean;
     limit: number;
     startCursor: `0x${string}`;
  };
  results: object[];
}>;
```

Defined in: [packages/sdk/src/api.ts:84](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/api.ts#L84)

Fetch comments from the Indexer API

## Parameters

### options

[`FetchCommentsOptions`](/sdk-reference/defaultExports/type-aliases/FetchCommentsOptions.mdx)

## Returns

`Promise`\<\{
  `extra`: \{
     `moderationEnabled`: `boolean`;
  \};
  `pagination`: \{
     `endCursor`: `` `0x${string}` ``;
     `hasNext`: `boolean`;
     `hasPrevious`: `boolean`;
     `limit`: `number`;
     `startCursor`: `` `0x${string}` ``;
  \};
  `results`: `object`[];
\}\>

A promise that resolves comments fetched from the Indexer API
