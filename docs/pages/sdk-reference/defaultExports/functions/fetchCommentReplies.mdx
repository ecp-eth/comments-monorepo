[**@ecp.eth/sdk**](../../index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / fetchCommentReplies

# Function: fetchCommentReplies()

```ts
function fetchCommentReplies(options): Promise<{
  extra: {
     moderationEnabled: boolean;
  };
  pagination: {
     endCursor: `0x${string}`;
     hasNext: boolean;
     hasPrevious: boolean;
     limit: number;
     startCursor: `0x${string}`;
  };
  results: object[];
}>;
```

Defined in: [packages/sdk/src/api.ts:211](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/api.ts#L211)

Fetch replies for a comment from the Indexer API

## Parameters

### options

[`FetchCommentRepliesOptions`](/sdk-reference/defaultExports/type-aliases/FetchCommentRepliesOptions.mdx)

## Returns

`Promise`\<\{
  `extra`: \{
     `moderationEnabled`: `boolean`;
  \};
  `pagination`: \{
     `endCursor`: `` `0x${string}` ``;
     `hasNext`: `boolean`;
     `hasPrevious`: `boolean`;
     `limit`: `number`;
     `startCursor`: `` `0x${string}` ``;
  \};
  `results`: `object`[];
\}\>

A promise that resolves replies fetched from the Indexer API
