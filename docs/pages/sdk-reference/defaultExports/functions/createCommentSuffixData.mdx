[**@ecp.eth/sdk**](/sdk-reference/index.mdx)

***

[@ecp.eth/sdk](/sdk-reference/index.mdx) / [defaultExports](/sdk-reference/defaultExports/index.mdx) / createCommentSuffixData

# Function: createCommentSuffixData()

> **createCommentSuffixData**(`__namedParameters`): `` `0x${string}` ``

Defined in: [packages/sdk/src/utils.ts:180](https://github.com/ecp-eth/comments-monorepo/blob/main/packages/sdk/src/utils.ts#L180)

Encode a comment to be used as a calldata suffix for a transaction

## Parameters

### \_\_namedParameters

#### appSignature

`` `0x${string}` ``

Signature of the app signer

#### authorSignature?

`` `0x${string}` ``

Signature of the author (not required if the author is the origin of the transaction)

#### commentData

\{ `appSigner`: `` `0x${string}` ``; `author`: `` `0x${string}` ``; `content`: `string`; `deadline`: `bigint`; `metadata`: `string`; `parentId`: `` `0x${string}` ``; `salt`: `` `0x${string}` ``; `targetUri`: `string`; \}

The content of the comment

#### commentData.appSigner

`` `0x${string}` `` = `HexSchema`

#### commentData.author

`` `0x${string}` `` = `HexSchema`

#### commentData.content

`string` = `...`

#### commentData.deadline

`bigint` = `...`

#### commentData.metadata

`string` = `...`

#### commentData.parentId

`` `0x${string}` `` = `HexSchema`

#### commentData.salt

`` `0x${string}` `` = `HexSchema`

#### commentData.targetUri

`string` = `...`

## Returns

`` `0x${string}` ``

The encoded comment data hex string
